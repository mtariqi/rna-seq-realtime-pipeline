// Minimal config for local runs. Customize executors for cluster/cloud.
params {
  outdir = 'results'
  workDir = './work'
}

profiles {
  docker {
    process.container = 'nfcore/rnaseq:latest'
  }
  singularity {
    process.container = 'docker://nfcore/rnaseq:latest'
  }
}
